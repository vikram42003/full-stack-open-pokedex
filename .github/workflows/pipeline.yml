name: Deployment pipeline
run-name: Deployment pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches: [main]
    types: [opened, synchronize]

jobs:
  simple_deployment_pipeline:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci

      - name: Check style
        run: npm run eslint -- --fix

      - name: Unit tests
        run: npm run test

      - name: build
        run: npm run build
      
      - name: Test Discord Webhook
        env:
          WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          curl -v -X POST -H "Content-Type: application/json" -d '{"content": "Debug test message"}' "$WEBHOOK_URL"
      

      - name: Discord Webhook Notify
        if: failure()
        uses: rjstone/discord-webhook-notify@v1.0.4
        with:
          severity: error
          description: Build failed
          details: commit ${{ github.event.head_commit.url }} by ${{ github.actor }} broke the build :(
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}

  discord-notify:
    needs: [simple_deployment_pipeline]
    runs-on: ubuntu-20.04
    steps:
      - name: Test Success!
        uses: rjstone/discord-webhook-notify@v1
        if: success()
        with:
          severity: info
          details: Test Succeeded!
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}
      - name: Test Failure!
        uses: rjstone/discord-webhook-notify@v1
        if: failure()
        with:
          severity: error
          details: Test Failed!
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}
      - name: Test Cancelled
        uses: rjstone/discord-webhook-notify@v1
        if: cancelled()
        with:
          severity: warn
          details: Test Cancelled!
          webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}

  # playwright_tests:
  #   needs: simple_deployment_pipeline
  #   uses: ./.github/workflows/playwright.yml
  #   with:
  #     node-version: "20"
    

  # deploy:
  #   runs-on: ubuntu-20.04
  #   needs: [simple_deployment_pipeline, playwright_tests]
  #   if: ${{ (github.event_name == 'push') && !contains(join(github.event.commits.*.message, ', '), '#SKIP') }}
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Bump version and push tag
  #       uses: anothrNick/github-tag-action@1.71.0
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #         DEFAULT_BUMP: patch
      
  #     - name: Trigger deployment
  #       run: curl https://api.render.com/deploy/srv-${{ secrets.RENDER_SERVICE_ID }}?key=${{ secrets.RENDER_API_KEY }}
      
  #     - name: Discord Webhook Notify
  #       uses: rjstone/discord-webhook-notify@v1.0.4
  #       with:
  #         severity: info
  #         description: A new version of Pokédex deployed
  #         details: to https://full-stack-open-pokedex-lbd4.onrender.com by ${{ github.actor }}
  #         webhookUrl: ${{ secrets.DISCORD_WEBHOOK }}